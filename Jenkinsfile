pipeline {
    agent any

    stages {
        stage("build") {
            script {
                echo "building application and image"
            }
        }
        stage("test") {
            script {
                echo "testing application"
            }
        }
        stage("deploy") {
            script {
                echo "deploying application and image"
            }
        }
    }
}

